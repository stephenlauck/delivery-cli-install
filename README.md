# delivery-cli-install

Test installation of delivery-cli across multiple versions and OS with test-kitchen.

## Usage

`kitchen converge`
`kitchen login`
`delivery -v`

### TODO
create tests to avoid logging in and testing manually
